$semantic-view Create a semantic view for Trading Analytics i.e, operational queries. The semantic view must include the SECURITIES, TRADES, MARKET_DATA tables from FSI_DEMO_DB.CAPITAL_MARKETS. The semantic view must include at least 1 filter and 3 verified queries. Deploy the semantic view in FSI_DEMO_DB.CAPITAL_MARKETS and check that it has no errors. Write the final tested, error free code (YAML and SQL) to my desktop.

Example worse prompts:

$semantic-view Write the SQL code to create a semantic view for Trading Analytics (operational queries). The semantic view must include the SECURITIES, TRADES, MARKET_DATA tables. The semtic view must include at least 1 filter and 3 verified queries.